请对{{#1736131915430.text#}}的中文 json 数据中的描述按照下列规则进行润色，并输出润色后的 json。
1. 保留原来的 json 的格式和每一个 json 字段。
1. 使用“你”，不要使用“您”来指代读者。
2. return_value以及其他字段的描述必须严格按照{{#context#}}里面的术语、固定用法进行描述，且术语上下文得统一、多次生成时也得使用同一术语，否则你会受到惩罚。
3. 在所有描述里面，包括 shortdesc、return_value，如果提及任何函数、枚举、结构体、类的名称，需要体现为代码格式，不要加其他链接，以下为一个示例：
### 示例
在调用 `create` 创建引擎时。
###
4. 如果一个参数既是输入、又是输出参数，该参数的desc描述需要分别解释输入、输出时所表示的含义。按照以下示例来描述：
### 示例
"parameters": [
    {
        "desc": "输入和输出参数，表示 `CodecCapInfo` 数组的大小。\n- 输入值：用户执行该方法时定义的 `CodecCapInfo` 的大小。\n- 输出值：方法执行完毕后输出的 `CodecCapInfo` 的大小。"
    }
],
###
5. 在描述参数时，如果参数的类型是一个具体的数据结构或枚举，desc的描述请按照这个方式来描述："参数的完整描述"，详见"参数类型"，按照以下示例来描述：
### 示例一
"parameters": [
    {
        "name": "options",
        "type": "ChannelMediaOptions",
        "desc": "频道媒体选项，详见 `ChannelMediaOptions`。"
    }
],
###
### 示例二
"parameters": [
    {
        "name": "errorCode",
        "type": "RTM_ERROR_CODE",
        "desc": "错误码，详见 `RTM_ERROR_CODE`。"
    }
],
###
注意：
- 如果参数 desc 中重复提及了数据类型，如：“AgoraRtmClientKit 对象，详见 AgoraRtmClientKit。”则删掉后面半句详见。
- 如果某个参数的类型是引用数据类型，也需要按照上述示例来描述
- 如果某个参数的类型是基本数据类型，如 int、string 等，则无需写“详见xxx”。
6. 如果一个api的api_name以“on“开头或者parent_class为“IRtcEngineEventHandler”，则该API为一个回调，请使用下列句式描述shortdesc：“xxx回调”。在其他字段的描述以“该回调xxx”来描述。
6. 结合 api_signature 和返回值的注释，根据实际情况对 return_value 字段中返回值的描述按照下列示例进行润色：
- 如果没有返回值，return_value 字段留空。
- 结合 api_signature 判断，如果返回值是 bool 类型，则分别解释 true 和 false 的含义，按照下列示例来描述：
### bool 类型的返回值示例
“- `true`：xx成功。\n- `false`：xx失败。”
###
- 结合 api_signature 判断，如果该方法的返回值为 0 和小于 0，按照下列示例来描述：
### 示例
- 0: 方法调用成功。\n- < 0: 方法调用失败。
###
- 结合 api_signature 判断，如果该方法的返回值是一个对象或实例，按照下列示例来描述：
### 示例
- 方法调用成功，返回xxx对象/实例。
###
注意：xxx 对象，xxx 需要添加 code font。
- 结合 api_signature 判断，如果该方法的返回值是一个指针型的返回值，按照下列示例来描述：
### 示例
- 方法调用成功，返回xxx。\n- 方法调用失败，返回空指针。
###
- 结合 api_signature 判断，如果该方法的返回值是一个字符串型的返回值，按照下列示例来描述：
### 示例
- 方法调用成功时，返回xxx。\n- 方法调用失败，返回空字符串。
###
-  结合 api_signature 判断，如果该方法的返回值是 int 型且不会返回负值，按照下列示例来描述：
### 示例
方法调用成功，返回 xxx。
###
7. 结合 api_signature 判断，如果 api 的返回值是一个接口类型对象、具体类对象、抽象类对象、数据传输对象，则在返回值描述时需要加上“详见xxx（具体对象）”，以下为一个示例：
### 示例
{  "api_signature":"virtual IRtmLock* getLock() = 0;",
    "return_value":"- 方法调用成功时，返回一个 `lock` 实例，详见 `IRtmLock`。\n- < `NULL`: 方法调用失败失败。"
}
###
8. 所有描述性文字均来源于代码注释，禁止自己编造，润色需要符合中文C++ API技术文档的风格，语言需要精简、清晰、逻辑通顺，通俗易懂。描述时确保信息对等即可，语言结构不一定需要跟原文保持一致。
9. 如果一个参数是输出参数，按照下列示例来描述：
###示例
"parameters": [
    {
        "desc": "输出参数，表示 `CodecCapInfo` 数组的大小。
    }
],
###
10. 如果注释使用的是bullet point，描述中需保留原有格式
11. 输出的 json 数据里面，请确保每个段落的信息独特且不重复，对于非必填项，如果原文没有这个信息则留空，不要编造
12. 如果 shortdesc 和 detailed_desc 里面的 desc 信息完全重复，则删除后者的描述，如果有部分重复，则删除后者里面部分重复的描述
13. 如果一个 API 是回调，在润色 shortdesc 的描述时，根据语境选择下列任意一种句式来描述：“xxx回调”或”报告xxxx“，以下为示例：
### 示例
- 远端视频状态发生改变回调。
- 远端用户成功加入频道时触发的回调。
- 报告人脸检测结果。
###
14. 在diff的注释中，如果在描述方法名时带有超链接，比如：\ref agora::rtc::IRtcEngine::joinChannel "joinChannel()"，直接处理为带代码格式的 api 名称即可，如 `joinChannel`
15. 如果参数的 desc 中有“锁”，请替换为 Lock
16. 如果 shortdesc 或 desc 中有“rtm”或者“Rtm”，请替换为 ”RTM“
17. 在描述时，把原文中 "xxx.{api 名称}" 替换为 “{api 名称}。
### 示例
“log.NewDefaultLogger”处理为“NewDefaultLogger”
18. 参数 desc 的描述不要跟参数 note 中的信息重复，如果二者信息重复，删除前者中信息重复的部分，否则你会受到惩罚
19. “详见 xxx”，“xxx” 需要添加 code font，即 `xxx`。
20. 结合 api_signature 判断，如果 api 没有任何参数，则 parameters 字段为空，否则你会受到惩罚
21. 对中文的润色语言需要自然流畅，符合中文地道表达