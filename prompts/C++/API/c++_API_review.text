按照下列规则对{{#1731492051004.text#}}的json数据结构中的描述进行 review，确保描述都按照下列规则来处理：
1. 保留原来的 json 的格式和每一个 json 字段。
1. 使用{{#context#}}中的术语和固定表达，尤其是返回值的描述，需要严格按照固定用法的示例来描述。
注意：如果同一个术语或者固定表达在一个 api 中多次出现，需要使用同一个用法。
2. 在所有描述里面，如果提及任何函数、枚举、结构体、参数的名称，需要加 code font，不要加其他链接。
注意：数字取值范围，如 [0, 100]，无需加 code font。
3. 描述中如果提及其他 API 名称并带有类名，仅保留 api 名。
4. 描述中如果提及其他 API 名称并带有()，仅保留 api 名。
5. 中文描述每句话结束后应有句号。中文中如果有英文，前后需要有一个英文空格。
6. 详见 xxx，xxx 需要添加 code font。
7. 参数 desc 的信息不要跟参数 note 中的信息重复，如果二者信息重复，删除前者中信息重复的部分。
8. shortdesc 和 detailed_desc 的信息不要互相重复，如果重复，删除后者中信息重复的部分。
9. 如果 api 没有任何参数，则 parameters 字段留空。
10. 如果某个参数的类型是基本数据类型，如 int、string、const std::string 等，则在参数描述desc中，不写“详见{参数类型}”。
11. 如果某个参数的类型是引用数据类型，在参数描述末尾增加 ”详见 {引用数据类型名称}"。注意：仅写参数名称，不要带任何其他东西。
12. 如果 change_desc 里面有“删除了 @technical preview”，则 change_type 修改为 create。
13. 返回值的描述如果符合固定用法中的描述，则遵循固定用法，如果不符合，则按照注释的描述来写。
14. 返回值的描述里面如果只有返回成功的情况，不要用 bullet point。返回值如果是 bool 型，直接解释
14. 如果注释使用的是bullet point，描述中需保留原有格式。
15. 如果原文注释中有外部文档超链接，需要保留。
16. 返回值的描述里面如果只有返回成功的情况，不要用 bullet point。返回值如果是 bool 型，直接解释 true/YES、false/NO 的含义，不要加“bool 型”。如果注释里有默认值，输出的参数描述需要标记（默认），禁止编造



