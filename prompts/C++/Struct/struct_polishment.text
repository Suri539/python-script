请对{{#1736131915430.text#}}的中文 json 数据中的描述按照下列规则进行润色，禁止改变 json 的格式、禁止删除任何 json 字段：
1. 使用“你”，不要使用“您”来指代读者。
2. json 字段的描述必须按照{{#context#}}里面的术语、固定用法进行描述，且术语上下文得统一、多次生成时也得使用同一术语，否则你会受到惩罚。
3. 在所有描述里面，包括 shortdesc、return_value，如果提及任何函数、枚举、结构体、类的名称，需要体现为代码格式，不要加其他链接，以下为一个示例：
### 示例
在调用 `create` 创建引擎时。
###
4. 如果一个参数既是输入、又是输出参数，该参数的desc描述需要分别解释输入、输出时所表示的含义。按照以下示例来描述：
### 示例
"parameters": [
    {
        "desc": "输入和输出参数，表示 `CodecCapInfo` 数组的大小。\n- 输入值：用户执行该方法时定义的 `CodecCapInfo` 的大小。\n- 输出值：方法执行完毕后输出的 `CodecCapInfo` 的大小。"
    }
],
###
5. 在描述参数时，如果参数的类型是一个具体的数据结构或枚举，desc的描述请按照这个方式来描述："参数的完整描述"，详见"参数类型"，按照以下示例来描述：
### 示例一
"parameters": [
    {
        "name": "options",
        "type": "ChannelMediaOptions",
        "desc": "频道媒体选项，详见 `ChannelMediaOptions`。"
    }
],
###
### 示例二
"parameters": [
    {
        "name": "errorCode",
        "type": "RTM_ERROR_CODE",
        "desc": "错误码，详见 `RTM_ERROR_CODE`。"
    }
],
###
注意：
- 如果参数 desc 中重复提及了数据类型，如：“AgoraRtmClientKit 对象，详见 AgoraRtmClientKit。”则删掉后面半句详见。
- 如果某个参数的类型是引用数据类型，也需要按照上述示例来描述
- 如果某个参数的类型是基本数据类型，如 int、string 等，则无需写“详见xxx”。
6. 在润色shortdesc和detailed_desc里面的描述时，直接介绍这个struct或class的功能即可，禁止添加冗余描述，例如“定义了xx”、“xx结构体”等。
7. 所有描述性文字均来源于代码注释，禁止自己编造，润色需要符合中文C++ API技术文档的风格，语言需要精简、清晰、逻辑通顺，通俗易懂。描述时确保信息对等即可，语言结构不一定需要跟原文保持一致。
8. 如果注释使用的是bullet point，描述中需保留原有格式
9. 输出的 json 数据里面，请确保每个段落的信息独特且不重复，对于非必填项，如果原文没有这个信息则留空，不要编造
10. 如果 shortdesc 和 detailed_desc 里面的 desc 信息完全重复，则删除后者的描述，如果有部分重复，则删除后者里面部分重复的描述
11. 如果{{#sys.query#}}中的描述是类名+方法名，例如`PlayerObserver.onStateChanged`，则在中文描述中统一处理为：xx类下的xx方法。例如：`PlayerObserver` 类下的 `onStateChanged` 方法
12. 在diff的注释中，如果在描述方法名时带有超链接，比如：\ref agora::rtc::IRtcEngine::joinChannel "joinChannel()"，直接处理为带代码格式的 api 名称即可，如 `joinChannel`
13. 如果参数的 desc 中有“锁”，请替换为 Lock
14. 如果 shortdesc 或 desc 中有“rtm”或者“Rtm”，请替换为 ”RTM“
15. 如果参数原文没有任何注释，则parameters里面desc留空，禁止编造
16. 在描述时，把原文中 "xxx.{api 名称}" 替换为 “{api 名称}。
### 示例
“log.NewDefaultLogger”处理为“NewDefaultLogger”
17. 参数 desc 的描述不要跟参数 note 中的信息重复，如果二者信息重复，删除前者中信息重复的部分，否则你会受到惩罚
18. “详见 xxx”，“xxx” 需要添加 code font，即 `xxx`。